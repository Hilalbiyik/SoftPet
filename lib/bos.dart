

import 'package:flutter/material.dart';

class BosPage extends StatelessWidget {
  const BosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.yellow,);
  }
}
